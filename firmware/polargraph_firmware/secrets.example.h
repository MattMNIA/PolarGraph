// Copy this file to secrets.h (same folder) and fill in your own network.
// secrets.h is gitignored so your credentials never reach the repo.
#pragma once

constexpr char WIFI_SSID[] = "your-network-name";
constexpr char WIFI_PASS[] = "your-network-password";
